name: Build & Deploy PWA (OIDC)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # required for OIDC
      contents: read

    env:
      AWS_REGION: ${{ vars.AWS_REGION || secrets.AWS_REGION }}
      S3_BUCKET: ${{ vars.S3_BUCKET || secrets.S3_BUCKET }}
      CF_DISTRIBUTION_ID: ${{ vars.CF_DISTRIBUTION_ID || secrets.CF_DISTRIBUTION_ID }}
      AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN || secrets.AWS_ROLE_ARN }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync to S3 (private bucket behind CloudFront OAC)
        run: aws s3 sync dist/ s3://$S3_BUCKET --delete

      - name: Who am I?
        run: aws sts get-caller-identity

      - name: Show CF env var (check for spaces/newlines)
        run: |
          echo "CF_DISTRIBUTION_ID='${CF_DISTRIBUTION_ID}'"
          printf 'LEN=%s\n' "${#CF_DISTRIBUTION_ID}"

      - name: Find distribution by ID (in this account)
        run: |
          aws cloudfront list-distributions \
            --query "DistributionList.Items[?Id=='${CF_DISTRIBUTION_ID}'].[Id,DomainName,Aliases.Items]" \
            --output table

      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id $CF_DISTRIBUTION_ID --paths "/*"
